import { Given, Then, When } from '../../../src'

Given('an order for {string}', (_thing: string) => {
  // no-op
})

When('an action', () => {
  // no-op
})

Then('an outcome', () => {
  // no-op
})
